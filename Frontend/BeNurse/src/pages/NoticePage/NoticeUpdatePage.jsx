import React from "react";
import { useParams } from "react-router-dom";

export default function NoticeUpdatePage() {
  const { noticeId } = useParams();
  return (
    <>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
      <div>NoticeUpdatePage {noticeId}</div>
    </>
  );
}
